<resources>

    <!-- Base application theme. -->
    <style name="BaseTextView" parent="AppTheme">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:padding">@dimen/padding_textview</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="TitleTextView" parent="BaseTextView">
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textSize">@dimen/title_size</item>
    </style>

    <style name="SubTitleTextView" parent="BaseTextView">
        <item name="android:textColor">@android:color/darker_gray</item>
        <item name="android:textSize">@dimen/subtitle_size</item>
        <item name="android:padding">0dp</item>
    </style>

    <style name="BlackSubTitleTextView" parent="SubTitleTextView">
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textSize">@dimen/subtitle_size</item>
        <item name="android:padding">@dimen/padding_soft</item>
    </style>

    <style name="PriceTextView" parent="BaseTextView">
        <item name="android:textColor">@color/theme_price_color</item>
    </style>

    <style name="BaseButton" parent="AppTheme">
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:padding">@dimen/padding_base_button</item>
        <item name="android:gravity">center</item>
    </style>

    <style name="ConfirmButton" parent="BaseButton">
        <item name="android:background">@drawable/button_confirm_selector</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:textStyle">italic</item>
    </style>

    <style name="AddToCartButton" parent="ConfirmButton">
        <item name="android:text">@string/book_catalog_add_to_cart</item>
    </style>

    <style name="AddedToCartButton" parent="ConfirmButton">
        <item name="android:text">@string/book_catalog_added_to_cart</item>
        <item name="android:background">@color/button_added_to_cart_background</item>
    </style>

    <style name="RetryButton" parent="BaseButton">
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">@dimen/error_message_size</item>
        <item name="android:text">@string/retry_text</item>
    </style>

</resources>
